# Tasks — AI 弱点フォーカス復習（Weakness Review）

## ステータス: ⏳ 未実装（承認待ち）

---

### Backend

- [ ] 1. `generate-reply` に弱点語彙取得ロジック追加
  - [ ] 1.1 `user_vocabulary` から `next_review_at <= now()` を最大3件取得
  - [ ] 1.2 `easiness_factor` 昇順でソート（苦手なものを優先）
  - [ ] 1.3 `buildSystemPrompt()` に弱点ブロックを注入
  - [ ] 1.4 レスポンスに `reviewedWords: string[]` フィールドを追加

- [ ] 2. `user_vocabulary` インデックス最適化
  - `CREATE INDEX ON user_vocabulary(user_id, character_id, next_review_at)`

### Flutter

- [ ] 3. `chat_provider.dart` — `reviewedWords` 受け取り処理
  - [ ] 3.1 返信データから `reviewedWords` を抽出
  - [ ] 3.2 各語彙を grade=3 で SM-2 更新

- [ ] 4. 語彙帳に「今日の復習」タブ追加
  - [ ] 4.1 `todayReviewProvider` 作成（next_review_at フィルター）
  - [ ] 4.2 タブ UI + 空状態（「今日の復習はありません 🎉」）
  - [ ] 4.3 「会話で復習済み」バッジ（今日の会話で出た語彙にマーク）

### 推定工数

- Backend: 2〜3時間
- Flutter: 2時間
- 合計: **4〜5時間**
